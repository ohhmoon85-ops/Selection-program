# 한영자장학재단 장학생 선발 시스템 — Vercel 배포용 의존 패키지
# Python 3.9+
# 설치: pip install -r requirements.txt

# ── Vercel Flask 백엔드 (api/index.py)
flask>=3.0.0
pandas>=2.0.0
PyMuPDF>=1.23.0
numpy>=1.24.0

# ── 로컬 Streamlit 실행용 (app.py, 선택)
# streamlit>=1.35.0
